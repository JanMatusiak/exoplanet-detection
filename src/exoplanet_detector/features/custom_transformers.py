'''
For your own transformers when sklearn built-ins are not enough.
Use this if you want to create custom preprocessing steps such as:
    dropping selected columns
    clipping impossible values
    custom log transform for left/right skew
    domain-specific feature creation
    safe handling of rare astronomy-specific columns
'''